en:
  activerecord:
    errors:
      models:
        reservation:
          attributes:
            ends_at:
              is_before_starts_at: can't be before start
        ride:
          attributes:
            ended_at:
              is_before_started_at: can't be before start
