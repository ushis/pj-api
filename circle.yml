---
machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t ushi/pj-api .
    - docker run -d --name postgres -e POSTGRES_PASSWORD=secret -e POSTGRES_DB=test postgres:9.5

database:
  override:
    - /bin/true

test:
  pre:
    - env | grep -P '^(CIRCLE|CI|COVERALLS)' > circle.env
    - echo 'MAIL_REPLY=pj <pj@reply.localhost.local>' >> circle.env
    - echo 'MAIL_FROM=pj <noreply@localhost.local>' >> circle.env
  override:
    - docker run --link postgres --env-file circle.env -e DATABASE_URL=postgres://postgres:secret@postgres/test ushi/pj-api bundle exec rake
    - docker run ushi/pj-api bundle exec brakeman -q -z

deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push ushi/pj-api
      - curl -XPOST -u $DEPLOY_USER:$DEPLOY_PASS $DEPLOY_HOOK
